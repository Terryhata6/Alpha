using UnityEngine;

namespace AlphaSource.Services.UI
{
    public class UIMediator : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
